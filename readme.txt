## Getting Started

```
npm i
npm start
```

cd C:\Users\SLE\.Cursorai\.cursor-tutor-main\projects\javascript
npx react-scripts start

Zum Deployen
npm run build

mkdir C:\Users\SLE\.Cursorai\tailwindproject\.tailwind
cd C:\Users\SLE\.Cursorai\tailwindproject\.tailwind
npm init -y

Cannot find module 'react'
= npm install react react-dom

npm install
npm start

     git
     # Füge neue/geänderte Dateien hinzu

git add .

# Erstelle einen neuen Commit

git commit -m "Beschreibe deine Änderungen hier"

# Lade die Änderungen zu GitHub hoch

git push

# Zeigt den Status deiner Dateien

git status

# Zeigt die Unterschiede in deinen Dateien

git diff

cd C:\Users\SLE\.Cursorai\tailwindproject

speichern git
git add .; git commit -m "Update $(Get-Date -Format 'dd.MM.yyyy')"; git push origin main

taskkill /F /IM node.exe
npm run dev

# builden und deployen
npm run build
netlify deploy --prod